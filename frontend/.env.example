# Copy to frontend/.env.local for local overrides (gitignored via root .env rules).
# Production: set VITE_API_URL in Cloudflare Pages → Settings → Environment variables.

VITE_API_URL=http://localhost:4010
