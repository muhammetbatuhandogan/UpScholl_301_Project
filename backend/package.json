{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 4010 --app-dir .",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 4010 --app-dir ."
  }
}
